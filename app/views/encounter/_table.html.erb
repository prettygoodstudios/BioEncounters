<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <% encounters.each do |e| %>
      <tr>
        <td><%= link_to e.date, encounter_path(e.id) %></td>
        <td><%= e.description %></td>
      </tr>
    <% end %>
  </tbody>
</table>
