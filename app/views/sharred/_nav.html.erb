<nav>
  <div class="nav-title-group">
    <div class="toggle-wrapper">
      <button id="toggle" class="toggle toggle__open">
        <div class="toggle-one"></div>
        <div class="toggle-two"></div>
        <div class="toggle-three"></div>
      </button>
    </div>
    <h1><%= link_to "<span>Bio</span>Vision".html_safe, root_path %></h1>
  </div>
  <div class="menu menu__hide">
    <div class="menu-item">
      <%= link_to "Species", specie_index_path %>
    </div>
    <% if current_user == nil %>
      <div class="menu-item"><%= link_to "Sign In", new_user_session_path %></div>
      <div class="menu-item"><%= link_to "Sign Up", new_user_registration_path%></div>
    <% else %>
      <div class="menu-item menu-profile">
        <div class="menu-profile-header">
          <div><%= current_user.display %></div>
          <div>
            <i class="fas fa-user-circle"></i>
            <span class="menu-profile-header__carot"></span>
          </div>
        </div>
        <div class="menu-profile-items">
          <%= link_to "Sign Out <i class='fas fa-sign-out-alt'></i>".html_safe, destroy_user_session_path, method: :delete, class: "menu-profile-item" %>
          <%= link_to "My Encounters <i class='fas fa-leaf'></i>".html_safe, "my_encounters/"+current_user.id.to_s, class: "menu-profile-item" %>
        </div>
      </div>
    <% end %>
  </div>
</nav>
